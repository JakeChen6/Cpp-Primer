// Define a map that associates words with a list of line numbers on which the word might occur.

#include <string>
using std::string;

#include <map>
using std::map;

#include <list>
using std::list;

int main()
{
    map<string, list<int>> word_lines_mapping;
    return 0;
}