`vector` is a sequential container and elements are sotred and accessed sequentially by their positions in the container.

`map` is an associative container and elements are sotred and retrieved by a key.