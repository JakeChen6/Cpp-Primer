// c1 and c2 must be of the same container type and have the same element type.
// Moreover, if the element type does not support the relational operator, we
// cannot compare two containers that hold that element type.