#include <iostream>
#include <vector>

using std::cin;
using std::cout;
using std::endl;
using std::vector;

bool compare(matrix &, matrix &);
vector<int>::iterator change_val(int, vector<int>::iterator);
